projection implementation in class ZBP_C_RSGAFLGT unique;
strict ( 2 );
use draft;
define behavior for ZRSG_C_Flight alias Flight
use etag

{

field (readonly)
   PlaneTypeID;

//  use create;
  use update;
//  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}